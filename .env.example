NEXT_PUBLIC_SUPABASE_URL=       # Supabase → Project Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=  # Supabase → Project Settings → API → anon/public
SUPABASE_SERVICE_ROLE_KEY=      # Supabase → Project Settings → API → service_role (server only)
ANTHROPIC_API_KEY=              # console.anthropic.com → API Keys (server only)
UPSTASH_REDIS_REST_URL=         # console.upstash.com → Redis DB → REST API URL
UPSTASH_REDIS_REST_TOKEN=       # console.upstash.com → Redis DB → REST API token
NEXT_PUBLIC_APP_URL=            # http://localhost:3000 locally; Vercel URL in production
FAL_KEY=                        # fal.ai → Dashboard → API Keys (server only)
FIRECRAWL_API_KEY=              # firecrawl.dev → Dashboard → API Keys (server only)
STRIPE_SECRET_KEY=              # stripe.com → Developers → API Keys → Secret key (server only)
STRIPE_WEBHOOK_SECRET=          # stripe.com → Developers → Webhooks → Signing secret (server only)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY= # stripe.com → Developers → API Keys → Publishable key
STRIPE_PRICE_PRO_MONTHLY=      # Stripe price ID for Pro monthly ($59/mo)
STRIPE_PRICE_PRO_ANNUAL=        # Stripe price ID for Pro annual ($590/yr)
STRIPE_PRICE_MAX_MONTHLY=      # Stripe price ID for Max monthly ($99/mo)
STRIPE_PRICE_MAX_ANNUAL=        # Stripe price ID for Max annual ($990/yr)
STRIPE_PRICE_PAY_PER_COURSE=   # Stripe price ID for Pay Per Course ($15 one-time)
